[build]
  command = "npm run build" 
  publish = "dist"

# Single redirect for SPA - simpler is better!
[[redirects]]
  from = "/app/*"
  to = "/app/index.html"
  status = 200
  force = true

# Book website fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200